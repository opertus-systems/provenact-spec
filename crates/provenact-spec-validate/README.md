# provenact-spec-validate

Schema-loading and validation helpers for Provenact JSON Schema contracts.

Use this crate when building Rust tooling that needs to validate manifests,
receipts, policy documents, and related vector fixtures.
